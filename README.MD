# This is only a user manual.

**General**
- When wanting to get new data on what students are doing, click the refresh button. Otherwise, the app does not update.

**Usage tips**
- Upon opening the app, you will see a table. You can search for an item in it using Control + F

**IMPORTANT (Port Information)**
- No two computer labs should *ever* share a port.
- In order to prevent the above from occuring, the first time the software is used, a teacher should change the port using the button on the main page. The password is ADMIN_RSegG4sp5BHjDv6KQJFEMmah9Vt3wZ (case sensitive).
- **Do not change ports while a computer in another computer lab is open and signed in**
- If ports are somehow mixed up, simply sign out all computers in one lab, sign in all computers in another, and change the port using the button under the controls tab. Do this for every computer lab that has mixed up ports.
- Log out then sign in all computers to let the port changes register.

**Security Precautions**
- Each message has an extra string of random text attatched to it, making it highly unlikely for a computer to accidently tamper with the system.
- Port reset messages are encrypted, so a user would not be able to decipher the password easily.
- In the event that the password is leaked, the result may not be catastrophic. The password is very long, complicated, and would not be easy to memorize at a glance. However, if the password really is leaked, without this software, the "hacker" would not be able to know which encryption method the password uses. However, try not to let the password become known. As if it is leaked, the only way to reset it would be to change the code on all computers (yikes, and not realistic).
